include ../modules/header
include ../modules/menu

h2 Unidades do curso
if(newUnit)
    span="Nova unidade!"
if(course.courseUnits)
    each courseUnit in course.courseUnits
        h2=courseComment.commentAuthor
        span=courseComment.commentDate
        p=courseComment.commentBody 

h3 Adicionar nova unidade
form(method="post" action="/cursos/"+course._id+"/unidades/")
    label(for="unitTitle") Título da unidade
    input(type="text" name="unitTitle")
    input(type="submit" value="Adicionar unidade")